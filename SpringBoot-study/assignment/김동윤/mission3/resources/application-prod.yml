
spring:
  config:
    activate:
      on-profile: deploy
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/produce_three_scheme?serverTimezone=UTC&characterEncoding=UTF-8
    username : demo_jpa
    passsword : Qpqpqp0614!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    database: sql-server
